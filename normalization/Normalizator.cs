using System;
using System.Collections.Generic;
using System.Diagnostics.CodeAnalysis;
using System.Linq;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading.Tasks;


namespace normalization
{
    public static class Normalizator
    {
       /*public static IEnumerable<T> CompressVerbs<T> (this IEnumerable<T> verbCollection, IDictionary<string, string> perfectVerbsDict) where T : VerbInfo
        {
            foreach(var verb in verbCollection)
            {
                var normalizeVerb = verb.ImperfectIntransitiveRule()
                    .PerfectIntransiveRule(perfectVerbsDict).PerfectIntransiveWithoutEndRule(perfectVerbsDict)
                    .PerfectTransitiveRule(perfectVerbsDict).ToList();
            }
        }*/

    }
}
